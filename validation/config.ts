// Shared validation configuration constants

export const DEFAULT_DIFF_IGNORE_PATHS: string[] = [
  'world.particles',      // Visual-only
  'world.animations',     // Visual-only
  'flags.debugMode',      // Dev-only
];
