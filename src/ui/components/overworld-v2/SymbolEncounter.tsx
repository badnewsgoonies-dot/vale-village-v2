import { FunctionalComponent } from 'preact';

// Minimal SymbolEncounter DOM component (not required for canvas render)
export const SymbolEncounter: FunctionalComponent<{ id: string; x: number; y: number }> = () => {
  return null;
};

export default SymbolEncounter;
